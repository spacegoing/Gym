{% set language = instance.repo_language | default('python') %}
I've uploaded a {{ language }} repository in the directory {{ workspace_path }}.

<issue>
{{ instance.problem_statement }}
</issue>

Implement the necessary changes to non-test files in {{ workspace_path }} to resolve this issue.
Test files have already been updated — do not modify them.

Remember: create your full written plan BEFORE making any code changes.